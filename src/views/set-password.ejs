<!DOCTYPE html>
<html lang="en">
<head>
	<%- include ('./partials/head.ejs') %> 
	<title>Set Password</title>
	<link href="/css/set-password.css" rel="stylesheet">
</head>
<body>

	<header>
		<%- include ('./partials/header.ejs') %> 
	</header>
	
	<main>
		<!-- SHOWS NOTIFICATION -->
		<% if (messages.notification) { %>
			<script>
				Toastify({
					text: '<%= messages.notification %>',
					backgroundColor: "linear-gradient(to right, #00b09b, #96c93d)",
					className: "info",
				}).showToast();
			</script>
		<% } %>
		<!-- SHOWS NOTIFICATION ENDS -->
		
		<section class="container-fluid my-4 p-4">
			<h1 id="heading">Set password</h1>
			<p class="lead">Hi <%= user.name %> 👋, you have not set your local password. Go ahead and set a new password ⌨️</p>
			
			<div class="set-password-form mt-5">
				<form name="set-password" method="POST" action="/auth/password/set" autocomplete="off" class="form" role="form">
					<div class="form-group">
						<input type="hidden" name="_csrf" value="<%= locals.csrfToken %>">
					</div>
					<div class="form-group">
						<input class="form-control" type="password" name="password" placeholder="Password" required>
					</div>
					<div class="form-group">
						<input class="form-control" type="password" name="confirmPassword" placeholder="Confirm password" required>
					</div>
					<button type="submit" class="btn btn-danger btn-sm">Submit</button>
				</form>
			</div>
		</section>

		<footer>
			<%- include ('./partials/footer.ejs') %> 
		</footer>

	</main>
</body>
</html>

